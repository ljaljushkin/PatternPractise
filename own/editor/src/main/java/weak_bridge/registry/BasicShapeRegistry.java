package weak_bridge.registry;

import weak_bridge.shape.Circle;
import weak_bridge.shape.Rectangle;
import weak_bridge.shape.Triangle;
import weak_bridge.shape.composite.CompositeShape;

public class BasicShapeRegistry {
    private ShapeRegistry instance = null;

    public BasicShapeRegistry() {

    }
}
